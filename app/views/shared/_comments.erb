<!--  start related-activities -->
<div id="related-activities">
  		
  <!--  start related-act-top -->
  <div id="related-act-top">
    <%= image_tag "../images/forms/header_related_act.gif", :size => "271x43" %>
  </div>
  <!-- end related-act-top -->
		
  <!--  start related-act-bottom -->
  <div id="related-act-bottom">
    <!--  start related-act-inner -->
	  <div id="related-act-inner">
	    <% @comments.each do |comment| %>
	      <div class="left"><%= image_tag "../images/forms/icon_edit.gif", :size => "21x21" %></div>
	      <div class="right">
	        <h5><%= comment.user.full_name if comment.user %></h5>
		      <%= comment.body %>
	      </div>
	      <div class="clear"></div>
	      <div class="lines-dotted-short"></div>
	    <% end %>
      <div class="comment-form">
        <% if current_user.is_a?(User) %>
          <%= render :partial => "comments/form" %>
        <% else %>
          <%= render :partial => "admins/comments/form" %>
        <% end %>
      </div>
    </div>
    <!-- end related-act-inner -->
    <div class="clear"></div>
  </div>
  <!-- end related-act-bottom -->

</div>
<!-- end related-activities -->
